{
  "name": "grz-api-rest",
  "version": "1.0.0",
  "description": "GRZ API Rest",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn --transpile-only -r tsconfig-paths/register src/server.ts",
    "debug": "tsc -p tsconfig.debug.json && tsc-alias -p tsconfig.debug.json",
    "build": "tsc -p tsconfig.json && tsc-alias -p tsconfig.json"
  },
  "author": "Eduardo Aihara",
  "license": "MIT",
  "dependencies": {
    "awilix": "^9.0.0",
    "awilix-express": "^8.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "tsc-alias": "^1.8.8",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.3.2"
  }
}
